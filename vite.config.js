import { defineConfig } from 'vite';

export default defineConfig({
  // GitHub Pagesで公開するためのベースURL設定です。
  // リポジトリ名を「neon-blocks」にする場合はこのままでOKです。
  // もし別の名前にする場合は、ここの名前も変更してください。
  base: '/neon-blocks/',
});
